import { TempratureConvertPipe } from './temprature-convert.pipe';

describe('TempratureConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new TempratureConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
